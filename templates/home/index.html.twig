{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<style>
    .accueil-container {
        max-width: 960px;
        margin: 2rem auto;
        padding: 2rem;
        background-color: #f8f9fa;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0,0,0,0.05);
    }

    .accueil-container h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
    }

    .accueil-container p {
        font-size: 1.1rem;
    }

    .btn-section {
        margin: 2rem 0;
    }

    .btn-section a {
        margin-right: 1rem;
        margin-bottom: 1rem;
    }

    .features {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
        margin-top: 2rem;
    }

    .feature-box {
        flex: 1 1 250px;
        background-color: white;
        border: 1px solid #dee2e6;
        border-radius: 8px;
        padding: 1rem;
        text-align: center;
    }

    @media (max-width: 600px) {
        .accueil-container {
            padding: 1rem;
        }

        .accueil-container h1 {
            font-size: 1.8rem;
        }
    }
</style>

<div class="accueil-container">
    <h1 class="text-center">Bienvenue sur notre application</h1>
    <p class="text-center">Cette application propose un système d'authentification complet avec Symfony.</p>
    <p class="text-center">Vous pouvez vous inscrire, vous connecter, gérer votre profil et bien plus encore.</p>

    <div class="text-center btn-section">
        {% if not app.user %}
            <a href="{{ path('app_register') }}" class="btn btn-primary">S'inscrire</a>
            <a href="{{ path('app_login') }}" class="btn btn-outline-secondary">Se connecter</a>
        {% else %}
            <a href="{{ path('app_profile') }}" class="btn btn-success">Mon profil</a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_admin') }}" class="btn btn-warning">Administration</a>
            {% endif %}
        {% endif %}
    </div>

    <div class="features">
        <div class="feature-box">
            <h5>🔒 Authentification sécurisée</h5>
            <p>Inscription, connexion et gestion des rôles utilisateurs.</p>
        </div>
        <div class="feature-box">
            <h5>📧 Vérification Email</h5>
            <p>Validation des comptes et réinitialisation du mot de passe.</p>
        </div>
        <div class="feature-box">
            <h5>👥 Gestion des utilisateurs</h5>
            <p>Administration des profils et des droits d'accès.</p>
        </div>
    </div>
</div>
{% endblock %}
