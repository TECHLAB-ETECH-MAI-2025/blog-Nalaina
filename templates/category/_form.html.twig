{{ form_start(form, { attr: { class: 'form-control', rows: 4 } }) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
