{% extends 'base.html.twig' %}

{% block title %}Chat avec {{ receiver.username }}{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('chat') }}
{% endblock %}

{% block body %}
<div class="container">
    <h2 class="mb-1">Chat avec {{ receiver.username }}</h2>

    <div id="chat-messages" class="border rounded p-3 mb-3 bg-light" style="height: 350px; overflow-y: auto;">

        {% include 'chat/messages.html.twig' %}
    </div>

    {{ form_start(form) }}
        <div class="input-group">
            {{ form_widget(form.content, {'attr': {'id': 'message-content','class': 'form-control', 'placeholder': 'Écrivez un message...'}}) }}
            <input type="hidden" id="receiver-id" value="{{ receiver.id }}">
            <button class="btn btn-primary" type="button" id="send-message">Envoyer</button>
        </div>
    {{ form_end(form) }}
</div>
{% endblock %}
